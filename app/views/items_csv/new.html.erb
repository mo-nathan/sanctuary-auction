<%= render "tabs", tab: "Items" %>

<h1>Upload Items CSV</h1>

<div class="alert alert-warning mt-3">
  <strong>Warning:</strong> This will delete ALL existing items and tags and replace them with the items from your CSV file. This action cannot be undone!
</div>

<div class="mt-3">
  <p>Upload a CSV file containing items to import. The CSV should include the following columns:</p>
  <ul>
    <li>Host, Title, Description, Details, Number, Time, Image</li>
    <li>Buyin (1 - Yes or 0 - No)</li>
    <li>Live auction? (Yes or No)</li>
    <li>Tag columns: Object_Mail, Object_Deliver, Object_Electronic, Event_InPerson, Event_Virtual, Event_Hybrid, Activity, Food, Artwork, Service, GroupEvent, Family</li>
  </ul>
</div>

<%= form_with url: preview_items_csv_path, method: :post, multipart: true, data: { turbo: false } do |form| %>
  <div class="mt-3">
    <%= form.label :csv_file, "Select CSV File:" %><br>
    <%= form.file_field :csv_file, accept: ".csv", required: true %>
  </div>

  <div class="mt-3">
    <%= form.submit "Preview Import", class: "btn btn-primary" %>
    <%= link_to "Cancel", items_path, class: "btn btn-secondary" %>
  </div>
<% end %>
