<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <a href="<%= user_path(user) %>">
        <%= user.name %>
      </a>
    </li>
  <% end %>
</ul>

<%= link_to "New User", new_user_path %>
