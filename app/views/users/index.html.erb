<h1>Users</h1>
<%= link_to "New User", new_user_path %>

<p>
  <ul>
    <% @users.each do |user| %>
      <li>
        <a href="<%= user_path(user) %>">
          <%= user.name %>
        </a>
	Balance: <%= user.balance %>
      </li>
    <% end %>
  </ul>
</p>
<%= link_to "Items", items_path %>
